language: java
jdk:
- oraclejdk7
# set a mysql database
before_install:
  - mysql -e "create database IF NOT EXISTS test;" -uroot
# whitelist
branches:
  only:
    - master
notifications:
  email: false
